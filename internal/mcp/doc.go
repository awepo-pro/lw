// Package mcp exposes the tool registry over the Model Context Protocol. It
// adds no tools of its own and changes no semantics — it is a transport,
// translating between canonical dotted tool names and MCP's underscored
// names. It will hold server.go and names.go.
package mcp
