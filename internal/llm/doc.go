// Package llm is an OpenAI-compatible streaming chat-completions client:
// request and message types, SSE streaming with tool-call accumulation, and
// a connectivity probe used by "lw doctor". It will hold types.go, client.go,
// stream.go and probe.go.
package llm
