// Package ui is the Bubble Tea v2 shell shared by every screen: the app
// model, key bindings, theme, pane layout, and the Pane interface that each
// screen implements. The shell never imports a screen; screens are injected
// through Options. It will hold app.go, keys.go, theme.go, layout.go and
// pane.go.
package ui
