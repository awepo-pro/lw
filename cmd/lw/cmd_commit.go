package main

import "errors"

// cmdCommit will commit the open changeset to the vault, refusing on lint
// regression unless --force is given.
func cmdCommit(args []string) error {
	return errors.New("not implemented yet")
}
