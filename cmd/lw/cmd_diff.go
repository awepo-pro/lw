package main

import "errors"

// cmdDiff will show the projected diff of the open changeset, optionally
// scoped to a single op.
func cmdDiff(args []string) error {
	return errors.New("not implemented yet")
}
