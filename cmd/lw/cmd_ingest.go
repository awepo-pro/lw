package main

import "errors"

// cmdIngest will ingest one or more sources (URL or local path) into raw/
// and propose the resulting pages as a changeset.
func cmdIngest(args []string) error {
	return errors.New("not implemented yet")
}
