── wiki/concepts/diff-target.md ────────────────────────────────────────────────

  Diff Target
  concept · inference · confidence medium · updated 2026-08-30

▎ Diff Target
▎
▎ See kv-cache and flash-attention for background.

── index.md ────────────────────────────────────────────────────────────────────

  Index

  Concepts

▎ • speculative-decoding — Drafting tokens with a small model and verifying them
▎   with the target model.
▎ • kv-cache — Caching attention keys/values across decoding steps.
▎ • flash-attention — Fused, memory-efficient attention kernel.
▎ • diff-target — Diff Target

  Entities

  • gpt-4 — OpenAI's GPT-4 model.

── wiki/concepts/kv-cache.md ───────────────────────────────────────────────────

  KV Cache
  concept · inference, memory · confidence high · updated 2026-08-29

  KV Cache

  The key/value cache stores per-layer attention projections from previous
  decoding steps so each new token only attends to cached keys and values
  instead of recomputing them.[kv-cache-explained.md]

  Why it matters

  Without caching, generating token n would repeat O(n) work already done for
  earlier tokens. Caching turns that into a constant amount of new work per
  step, at the cost of memory that grows with sequence length.

  Example

  A cache-hit log line looks like the block below. The ##-looking line inside it
  is plain text, not a heading, because it sits inside a fenced code block.

    ## this is not a heading, just log text
    kv_cache_hit_rate: 0.94

  Related

▎ • gpt-4 — an unrelated edit for TestDiffRender's fixture.
▎ • flash-attention — a kernel design that reduces the memory-bandwidth cost of
▎   reading the cache.
▎ • speculative-decoding — both the draft and target model read the cache during
▎   verification.
