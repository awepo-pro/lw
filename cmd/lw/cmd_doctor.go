package main

import "errors"

// cmdDoctor will check vault and lock health, optionally breaking a stale
// lock with --unlock.
func cmdDoctor(args []string) error {
	return errors.New("not implemented yet")
}
